virt-wrapper
============

virt-wrapper is a wrapper for virtualization applications. It warns users
if they are not a member of virt group and loads the kernel modules
automatically.

Currently kvm and VirtualBox are supported.

Requirements
------------
 - comar-api >= 2.4.3
 - Comar scripts for the following models:
    - Boot.Modules
    - User.Manager
 - PyQt 4 (optional)
