--- configure	2008-06-18 02:05:57.000000000 +0300
+++ configure-new	2008-06-18 02:26:04.000000000 +0300
@@ -7009,176 +7009,6 @@
 
 fi
 
-pkg_failed=no
-{ echo "$as_me:$LINENO: checking for MEDIA" >&5
-echo $ECHO_N "checking for MEDIA... $ECHO_C" >&6; }
-
-if test -n "$PKG_CONFIG"; then
-    if test -n "$MEDIA_CFLAGS"; then
-        pkg_cv_MEDIA_CFLAGS="$MEDIA_CFLAGS"
-    else
-        if test -n "$PKG_CONFIG" && \
-    { (echo "$as_me:$LINENO: \$PKG_CONFIG --exists --print-errors \"\\
-			glib-2.0 >= 1.3.7		\\
-			gmodule-2.0 >= 1.3.7		\\
-			libgnome-2.0 >= 2.13.7		\\
-			libgnomeui-2.0 >= 2.13.2		\\
-			esound >= 0.2.23\"") >&5
-  ($PKG_CONFIG --exists --print-errors "\
-			glib-2.0 >= 1.3.7		\
-			gmodule-2.0 >= 1.3.7		\
-			libgnome-2.0 >= 2.13.7		\
-			libgnomeui-2.0 >= 2.13.2		\
-			esound >= 0.2.23") 2>&5
-  ac_status=$?
-  echo "$as_me:$LINENO: \$? = $ac_status" >&5
-  (exit $ac_status); }; then
-  pkg_cv_MEDIA_CFLAGS=`$PKG_CONFIG --cflags "\
-			glib-2.0 >= 1.3.7		\
-			gmodule-2.0 >= 1.3.7		\
-			libgnome-2.0 >= 2.13.7		\
-			libgnomeui-2.0 >= 2.13.2		\
-			esound >= 0.2.23" 2>/dev/null`
-else
-  pkg_failed=yes
-fi
-    fi
-else
-	pkg_failed=untried
-fi
-if test -n "$PKG_CONFIG"; then
-    if test -n "$MEDIA_LIBS"; then
-        pkg_cv_MEDIA_LIBS="$MEDIA_LIBS"
-    else
-        if test -n "$PKG_CONFIG" && \
-    { (echo "$as_me:$LINENO: \$PKG_CONFIG --exists --print-errors \"\\
-			glib-2.0 >= 1.3.7		\\
-			gmodule-2.0 >= 1.3.7		\\
-			libgnome-2.0 >= 2.13.7		\\
-			libgnomeui-2.0 >= 2.13.2		\\
-			esound >= 0.2.23\"") >&5
-  ($PKG_CONFIG --exists --print-errors "\
-			glib-2.0 >= 1.3.7		\
-			gmodule-2.0 >= 1.3.7		\
-			libgnome-2.0 >= 2.13.7		\
-			libgnomeui-2.0 >= 2.13.2		\
-			esound >= 0.2.23") 2>&5
-  ac_status=$?
-  echo "$as_me:$LINENO: \$? = $ac_status" >&5
-  (exit $ac_status); }; then
-  pkg_cv_MEDIA_LIBS=`$PKG_CONFIG --libs "\
-			glib-2.0 >= 1.3.7		\
-			gmodule-2.0 >= 1.3.7		\
-			libgnome-2.0 >= 2.13.7		\
-			libgnomeui-2.0 >= 2.13.2		\
-			esound >= 0.2.23" 2>/dev/null`
-else
-  pkg_failed=yes
-fi
-    fi
-else
-	pkg_failed=untried
-fi
-
-
-
-if test $pkg_failed = yes; then
-
-if $PKG_CONFIG --atleast-pkgconfig-version 0.20; then
-        _pkg_short_errors_supported=yes
-else
-        _pkg_short_errors_supported=no
-fi
-        if test $_pkg_short_errors_supported = yes; then
-	        MEDIA_PKG_ERRORS=`$PKG_CONFIG --short-errors --errors-to-stdout --print-errors "\
-			glib-2.0 >= 1.3.7		\
-			gmodule-2.0 >= 1.3.7		\
-			libgnome-2.0 >= 2.13.7		\
-			libgnomeui-2.0 >= 2.13.2		\
-			esound >= 0.2.23"`
-        else
-	        MEDIA_PKG_ERRORS=`$PKG_CONFIG --errors-to-stdout --print-errors "\
-			glib-2.0 >= 1.3.7		\
-			gmodule-2.0 >= 1.3.7		\
-			libgnome-2.0 >= 2.13.7		\
-			libgnomeui-2.0 >= 2.13.2		\
-			esound >= 0.2.23"`
-        fi
-	# Put the nasty error message in config.log where it belongs
-	echo "$MEDIA_PKG_ERRORS" >&5
-
-	{ { echo "$as_me:$LINENO: error: Package requirements (\
-			glib-2.0 >= 1.3.7		\
-			gmodule-2.0 >= 1.3.7		\
-			libgnome-2.0 >= 2.13.7		\
-			libgnomeui-2.0 >= 2.13.2		\
-			esound >= 0.2.23) were not met:
-
-$MEDIA_PKG_ERRORS
-
-Consider adjusting the PKG_CONFIG_PATH environment variable if you
-installed software in a non-standard prefix.
-
-Alternatively, you may set the environment variables MEDIA_CFLAGS
-and MEDIA_LIBS to avoid the need to call pkg-config.
-See the pkg-config man page for more details.
-" >&5
-echo "$as_me: error: Package requirements (\
-			glib-2.0 >= 1.3.7		\
-			gmodule-2.0 >= 1.3.7		\
-			libgnome-2.0 >= 2.13.7		\
-			libgnomeui-2.0 >= 2.13.2		\
-			esound >= 0.2.23) were not met:
-
-$MEDIA_PKG_ERRORS
-
-Consider adjusting the PKG_CONFIG_PATH environment variable if you
-installed software in a non-standard prefix.
-
-Alternatively, you may set the environment variables MEDIA_CFLAGS
-and MEDIA_LIBS to avoid the need to call pkg-config.
-See the pkg-config man page for more details.
-" >&2;}
-   { (exit 1); exit 1; }; }
-elif test $pkg_failed = untried; then
-	{ { echo "$as_me:$LINENO: error: The pkg-config script could not be found or is too old.  Make sure it
-is in your PATH or set the PKG_CONFIG environment variable to the full
-path to pkg-config.
-
-Alternatively, you may set the environment variables MEDIA_CFLAGS
-and MEDIA_LIBS to avoid the need to call pkg-config.
-See the pkg-config man page for more details.
-
-To get pkg-config, see <http://pkg-config.freedesktop.org/>.
-See \`config.log' for more details." >&5
-echo "$as_me: error: The pkg-config script could not be found or is too old.  Make sure it
-is in your PATH or set the PKG_CONFIG environment variable to the full
-path to pkg-config.
-
-Alternatively, you may set the environment variables MEDIA_CFLAGS
-and MEDIA_LIBS to avoid the need to call pkg-config.
-See the pkg-config man page for more details.
-
-To get pkg-config, see <http://pkg-config.freedesktop.org/>.
-See \`config.log' for more details." >&2;}
-   { (exit 1); exit 1; }; }
-else
-	MEDIA_CFLAGS=$pkg_cv_MEDIA_CFLAGS
-	MEDIA_LIBS=$pkg_cv_MEDIA_LIBS
-        { echo "$as_me:$LINENO: result: yes" >&5
-echo "${ECHO_T}yes" >&6; }
-	:
-fi
-host=`uname -s`
-case "$host" in
-*SunOS*)
-	MEDIA_LIBS="$MEDIA_LIBS -ldevinfo -lX11"
-        ;;
-esac
-
-
-
-
 # Check for the CDDBSlave2 modules
 PROGRAMS_CDDBSLAVE=""
 # Check whether --enable-cddbslave was given.
@@ -8310,56 +8140,6 @@
   gst010_toolsdir=`$PKG_CONFIG --variable=toolsdir gstreamer-0.10`
   gst010_inspect="$gst010_toolsdir/gst-inspect-0.10"
 
-    for base_element in playbin audioconvert audioresample
-  do
-    { echo "$as_me:$LINENO: checking for GStreamer 0.10 $base_element plugin" >&5
-echo $ECHO_N "checking for GStreamer 0.10 $base_element plugin... $ECHO_C" >&6; }
-    if $gst010_inspect $base_element >/dev/null 2>/dev/null; then
-      { echo "$as_me:$LINENO: result: yes" >&5
-echo "${ECHO_T}yes" >&6; }
-    else
-      { echo "$as_me:$LINENO: result: no" >&5
-echo "${ECHO_T}no" >&6; }
-      { echo "$as_me:$LINENO: WARNING:
-                    Cannot find required GStreamer-0.10 plugin '$base_element'.
-                    It should be part of gst-plugins-base. Please install it.
-                    You need it installed for the sound recorder to work.
-      " >&5
-echo "$as_me: WARNING:
-                    Cannot find required GStreamer-0.10 plugin '$base_element'.
-                    It should be part of gst-plugins-base. Please install it.
-                    You need it installed for the sound recorder to work.
-      " >&2;}
-      PROGRAMS_GRECORD=""
-      PROGRAMS_GRECORD_ERROR="Missing gst-plugins-base '$base_element' element\n"
-    fi
-  done
-
-
-    for good_element in gconfaudiosink gconfaudiosrc
-  do
-    { echo "$as_me:$LINENO: checking for GStreamer 0.10 $good_element plugin" >&5
-echo $ECHO_N "checking for GStreamer 0.10 $good_element plugin... $ECHO_C" >&6; }
-    if $gst010_inspect $good_element >/dev/null 2>/dev/null; then
-      { echo "$as_me:$LINENO: result: yes" >&5
-echo "${ECHO_T}yes" >&6; }
-    else
-      { echo "$as_me:$LINENO: result: no" >&5
-echo "${ECHO_T}no" >&6; }
-      { echo "$as_me:$LINENO: WARNING:
-                   Cannot find required GStreamer-0.10 plugin '$good_element'.
-                   It should be part of gst-plugins-good. Please install it.
-                   You need it installed for the sound recorder to work.
-      " >&5
-echo "$as_me: WARNING:
-                   Cannot find required GStreamer-0.10 plugin '$good_element'.
-                   It should be part of gst-plugins-good. Please install it.
-                   You need it installed for the sound recorder to work.
-      " >&2;}
-      PROGRAMS_GRECORD=""
-      PROGRAMS_GRECORD_ERROR="Missing gst-plugins-good '$good_element' element\n"
-    fi
-  done
 else
   if test "x$HAVE_GST" = "xno"; then
     { echo "$as_me:$LINENO: WARNING: You need GStreamer installed for the mixer and recorder to work" >&5
@@ -8616,15 +8396,6 @@
   if test "$ESD_CONFIG" = "no" ; then
     no_esd=yes
   else
-    ESD_CFLAGS=`$ESD_CONFIG $esdconf_args --cflags`
-    ESD_LIBS=`$ESD_CONFIG $esdconf_args --libs`
-
-    esd_major_version=`$ESD_CONFIG $esd_args --version | \
-           sed 's/\([0-9]*\).\([0-9]*\).\([0-9]*\)/\1/'`
-    esd_minor_version=`$ESD_CONFIG $esd_args --version | \
-           sed 's/\([0-9]*\).\([0-9]*\).\([0-9]*\)/\2/'`
-    esd_micro_version=`$ESD_CONFIG $esd_config_args --version | \
-           sed 's/\([0-9]*\).\([0-9]*\).\([0-9]*\)/\3/'`
     if test "x$enable_esdtest" = "xyes" ; then
       ac_save_CFLAGS="$CFLAGS"
       ac_save_LIBS="$LIBS"
